{
  "name": "ton-deposit-bot",
  "version": "1.0.0",
  "description": "TON Deposit Monitoring Bot - Auto-creates investments when users deposit to platform wallet",
  "main": "dist/tonDepositBot.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/tonDepositBot.js",
    "dev": "ts-node tonDepositBot.ts",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "firebase": "^10.8.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
